!!!5
%html
  %head{:charset => "UTF-8"}
    %title= #{Site.first.name} Admin
    = stylesheet_link_tag :master, :media => "screen, projection"
    = stylesheet_link_tag 'plupload/jquery.plupload.queue/css/jquery.plupload.queue'
   
    = javascript_include_tag :application
    = javascript_include_tag :externals  
    
    = stylesheet_link_tag :externals

    = csrf_meta_tag
  %body{:class => body_id_set}
  #wrapper
    #header
      %h1#site_name
        = link_to "#{Site.first.name.titleize} Site", "/" 
      = render :partial => "layouts/admin/welcome"
    #main
      = render :partial => "layouts/admin/nav"
      #content
        = yield
  #footer
   

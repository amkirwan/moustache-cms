- header_content 'shared/header_wrapper', :object => @user

- inner_content "shared/inner_wrapper" do
  = render :partial => "form",  
           :locals => { :user => @user, :button_label => "Create User" } 

- if can? :create, Article
  %div{:id => "new_article", :class => "actions" }
    %ul.submenu
      %li.top_menu
        Actions<span class="action_arrow"></span>
        %ul 
          - if can? :create, @article_collection
            %li
              = link_to "New Article", [:new, :admin, @article_collection, :article]
          - if can? :update, @article_collection
            %li
              = link_to "Edit Collection Props", [:edit, :admin, @article_collection]
          - if can? :destroy, @article_collection
            %li
              = link_to "Delete Collection", [:admin, @article_collection], :confirm => "Are you sure you want to delete the article collection #{@article_collection.name}", :method => :delete
 

#asset_collections
  #h3 Asset Collections
  %ul
    - @asset_collections.each do |collection|
      %li{ :id => "#{collection.name}"}
        %strong
          = link_to collection.name, [:admin, collection]
        %div
          = link_to "delete collection", [:admin, collection], :method => :delete, :confirm => "Are you sure you want to delete this collection?", :class => "delete"
  
%div
  %ul#new_asset_collection
    %li= link_to "New Collection", [:new, :admin, :asset_collection]
#page_meta_data{:class => "foldable"}
  %fieldset
    %legend Page Meta Data<span class="fold_arrow rotate"></span>
    %ul.form_fields{:style => "display: none"}
      %li
        %h5 Default Meta Data
      = f.fields_for :default_meta_data, hash_to_open_struct(@page.default_meta_data) do |os|
        - os.object.marshal_dump.each_key do |key|
          %li  
            = os.label key
            = os.text_field key
          

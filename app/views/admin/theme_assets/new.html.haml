- header_content 'shared/header_wrapper', :object => :ThemeAsset do
  = can_create? ThemeAsset

- inner_content "shared/inner_wrapper" do
  = render :partial => "form",  
           :locals => { :button_label => "Save Theme Asset" }

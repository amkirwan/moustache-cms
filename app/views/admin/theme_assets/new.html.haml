- header_content 'shared/header_wrapper', :object => @users do
  = can_create? User

- inner_content "shared/inner_wrapper" do
  = render :partial => "form",  
           :locals => { :button_label => "Save Theme Asset" }

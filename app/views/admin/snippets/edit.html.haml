- header_content 'shared/header_wrapper', :object => @snippet do
  = can_destroy? @snippet

- inner_content "shared/inner_wrapper" do
  %div.breadcrumb
    = link_to "&laquo; Snippets".html_safe, [:admin, :snippets]

  = render :partial => "form",  
           :locals => { :button_label => "Update Snippet" }

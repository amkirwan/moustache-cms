%tr{:id => "tc_#{theme_collection.name.parameterize('_')}", :class => "#{cycle('even', 'odd')}"}
  %td.name= link_to theme_collection.name, [:admin, theme_collection]
  - if can? :destroy, theme_collection
    %td.delete= link_to "Delete", [:admin, theme_collection], :method => :delete, :data => { :confirm => "Are you sure you want to delete the asset collection #{theme_collection.name}?", :class => "delete" }

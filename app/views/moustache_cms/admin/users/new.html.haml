- header_content 'shared/header_wrapper', :object => @user

- inner_content "shared/inner_wrapper" do
  %div.breadcrumb
    = link_to "&laquo; Users".html_safe, [:admin, :users]

  = render :partial => "form",  
           :locals => { :button_label => "Create User" } 

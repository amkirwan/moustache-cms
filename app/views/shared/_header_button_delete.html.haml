%div{:id => "delete_#{class_name}", :class => "delete"}
  = link_to "Delete #{title}", [:admin, object], :confirm => "Are you sure you want to delete the #{class_name} #{name_or_title(object)}", :method => :delete, :class => "action delete"

%div{:id => "delete_#{class_name}", :class => "delete"}
  = link_to "Delete #{title}", [:admin, header_button], :confirm => "Are you sure you want to delete the #{class_name} #{name_or_title(object)}", :method => :delete, :class => "action delete"

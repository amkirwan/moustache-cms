%ul.nav-primary
  - homepage.children.each do |page|
    - if page.published?
      %li
        = link_to page.title, page.full_path, {:class => "nav nav-primary-#{page.title.downcase.gsub(/\s/, '_')}" } 
